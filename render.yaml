services:
  - type: web
    name: anthola-backend
    env: node
    plan: free
    buildCommand: cd anthola_fullstack/backend && npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        fromDatabase: database